# What2Eat

## How to Deploy

### Run from Source
Run the application directly from source code:

python3 gui_new.py

### Build Application
To run as an executable, use:

pyinstaller what2eat.spec

Run this command from the root of the repository. This will populate the /dist folder with the built application.
